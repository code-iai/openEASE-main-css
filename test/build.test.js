test('Should build valid layout.css', () => {
    //Read layout_original.css as string
    //Read layout.css as string
    //cmp those to strings
    //expect(layout_original_text).toBe(layout_text);
    expect(true).toBe(true);
});